# 开发起步与骨架记录（Sprint 0）

> 记录日期：2026-07-13
> 对应：`docs/10-开发准备与Sprint0.md`

---

## 一、环境差异记录

| 文档要求 | 实际环境 | 调整说明 |
|---------|---------|---------|
| .NET 8 SDK | .NET 9.0 SDK | 本地仅安装 9.0，项目生成 net9.0，使用 EF Core 9.0.4 |
| Visual Studio 2022 模板新建 | `dotnet new mvc` CLI 创建 | 等价，目录结构一致 |
| 包管理器控制台迁移 | `dotnet ef` CLI 迁移 | 等价 |

## 二、Sprint 0 交付物清单

| # | 交付物 | 状态 | 说明 |
|---|--------|------|------|
| 1 | `.sln` 解决方案文件 | ✅ | `LibrarySeatReservation.sln` |
| 2 | `.csproj` 项目文件 | ✅ | `src/LibrarySeatReservation.Web/LibrarySeatReservation.Web.csproj` |
| 3 | `Program.cs` | ✅ | 注册了 DbContext + Session + Service + Seed |
| 4 | `appsettings.json` | ✅ | LocalDB 连接字符串 `LibrarySeatDb` |
| 5 | Entity 类（3 个） | ✅ | Seat.cs, Reservation.cs, Admin.cs（与 doc 08 一致） |
| 6 | `AppDbContext` | ✅ | Fluent API 配置完整（主键、索引、默认值、外键 Restrict） |
| 7 | 首次迁移 | ✅ | `InitialCreate`，生成的迁移文件与 doc 08 DDL 一致 |
| 8 | 首次建表 | ✅ | `Update-Database` 成功，3 张表已创建 |
| 9 | `SeedData.cs` | ✅ | 1 个管理员（admin/admin123）+ 20 个座位（2 楼 + 3 楼） |
| 10 | `dotnet build` 通过 | ✅ | 0 错误 0 警告 |
| 11 | `dotnet run` 通过 | ✅ | 浏览器可访问首页 |

## 三、已创建文件清单

### Models/Entities/（3 文件）
- `Seat.cs` — 座位实体（Id, Floor, Area, SeatNumber, IsEnabled）
- `Reservation.cs` — 预约实体（Id, SeatId, UserName, StartTime, EndTime, Status, CreatedAt）
- `Admin.cs` — 管理员实体（Id, Username, Password）

### Models/（1 文件）
- `AppDbContext.cs` — DbContext + Fluent API 配置

### Models/ViewModels/（8 文件）
- `HomeIndexViewModel.cs` + `UserCard`
- `SeatsIndexViewModel.cs` + `SeatDisplayVM`
- `SeatDetailViewModel.cs` + `TimeSlotVM`
- `ReservationCreateViewModel.cs`
- `MyReservationsViewModel.cs`
- `LoginViewModel.cs`
- `AdminReservationsViewModel.cs`
- `AdminSeatsViewModel.cs`
- `SeatCreateViewModel.cs` + `SeatEditViewModel`
- `AdminStatisticsViewModel.cs`

### Services/（4 文件）
- `ISeatService.cs` — 接口
- `SeatService.cs` — 实现（含冲突检测、时段计算等）
- `IReservationService.cs` — 接口（含统计接口）
- `ReservationService.cs` — 实现（含冲突检测、状态流转、统计）

### Controllers/（5 文件）
- `HomeController.cs` — 首页（身份选择/仪表板）
- `AccountController.cs` — 切换用户（Session 写入/清除）
- `SeatsController.cs` — 座位列表 + 详情
- `ReservationController.cs` — 预约提交 + 我的预约 + 取消

### Controllers/Admin/（3 文件）
- `LoginController.cs` — 管理员登录（含属性路由）
- `ReservationsController.cs` — 预约管理（含属性路由）
- `SeatsController.cs` — 座位管理（含属性路由）
- `StatisticsController.cs` — 统计页（含属性路由）

### Views/（12 文件）
- `Shared/_Layout.cshtml` — 用户端布局（Bootstrap 5 + Session 用户名）
- `Home/Index.cshtml` — 首页（身份选择卡片 / 仪表板概览）
- `Seats/Index.cshtml` — 座位列表（楼层筛选 + 空状态）
- `Seats/Detail.cshtml` — 座位详情（时段占用图）
- `Reservation/Create.cshtml` — 预约表单（时段多选 + JS 校验）
- `Reservation/My.cshtml` — 我的预约（+ 空状态引导）
- `Admin/Shared/_Layout.cshtml` — 管理端布局（侧边栏 + 自适应）
- `Admin/Login.cshtml` — 管理员登录（独立 Layout）
- `Admin/Reservations.cshtml` — 预约管理（状态筛选 + 空状态）
- `Admin/Seats.cshtml` — 座位管理（Modal 新增 + 空状态）
- `Admin/SeatEdit.cshtml` — 编辑座位（独立页面）
- `Admin/Statistics.cshtml` — 统计页（4 卡片 + 空状态）

### Data/（1 文件）
- `SeedData.cs` — 种子数据（管理员 + 20 座位）

### Config（3 文件）
- `Program.cs` — 应用入口（DI + Session + Migrate + Seed）
- `appsettings.json` — 连接字符串
- `wwwroot/css/site.css` — 自定义样式

## 四、骨架阶段未覆盖项

| 未覆盖 | 原因 | 预期 Sprint |
|--------|------|------------|
| 完成业务逻辑 | 骨架阶段只写空框架 + 基本实现 | Sprint 1~2 |
| 统计页完整数据 | 已实现基本统计 Service 和 View，需真实预约数据 | Sprint 2 |
| 管理端 Session 超时自动跳转过滤器 | 暂未实现 `OnActionExecuting` 基类过滤器 | Sprint 3 |
| N+1 查询优化（批量占闲查询） | 见 doc 09 §2.3.3 说明 | Sprint 3 |

## 五、默认补足项

| 补足项 | 说明 |
|--------|------|
| .NET 9.0 替代 8.0 | 本地 SDK 环境限制 |
| EF Core 9.0.4 替代 8.x | 对应 .NET 9.0 |
| `FirstOrDefaultAsync` 需要 `using Microsoft.EntityFrameworkCore` | 在 Controller 中额外添加 |
| Admin `SeatsController.Edit` 实现 GET+POST 双方法 | 设计文档只写了 POST，骨架补充 GET 加载现有数据 |
