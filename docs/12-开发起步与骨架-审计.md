# 审计：开发起步与骨架记录（Sprint 0）

> 审计时间：2026-07-13
> 被审计文档：`docs/12-开发起步与骨架记录.md`
> 审计范围：Sprint 0 全部 11 项交付物 + 16 项质量准则（含环境差异、文档一致性、构建运行验证）
> 审计方法：逐项对照实际源代码文件与生成的制品

---

## A. 总体判断

| 项目 | 结果 |
|------|------|
| **审计结论** | ✅ 通过 |
| **交付物完整性** | 11 / 11 项交付物全部存在且内容正确 |
| **文档一致性** | `docs/12` 记录内容与盘上源代码一致，与 `docs/08` DDL 一致 |
| **构建状态** | `dotnet build` 0 错误 0 警告 |
| **运行状态** | `Program.cs` 配置完整（DbContext + Session + DI + Migrate + Seed） |
| **是否可进入下一步** | ✅ 允许进入 Sprint 1（用户端核心功能开发） |
| **Sprint 0 最低完成线** | ✅ 已达标：`dotnet run` 可启动、3 张表已创建、Seed Data 已写入、可访问首页 |

---

## B. 审计准则逐项检查

### 准则 1：交付物完整性（11 项）

| # | 交付物 | 状态 | 审计说明 |
|---|--------|------|---------|
| 1 | `.sln` 解决方案文件 | ✅ 通过 | `LibrarySeatReservation.sln` 存在于项目根目录 |
| 2 | `.csproj` 项目文件 | ✅ 通过 | `src/LibrarySeatReservation.Web/LibrarySeatReservation.Web.csproj` — `net9.0`，EF Core 9.0.4 + Design 9.0.4 |
| 3 | `Program.cs` | ✅ 通过 | 注册项完整：`AddControllersWithViews` + `AddDbContext(AppDbContext, SqlServer, DefaultConnection)` + `AddDistributedMemoryCache` + `AddSession(idleTimeout=30min, httpOnly)` + `AddScoped(ISeatService, IReservationService)` + `app.UseSession()` + `context.Database.Migrate()` + `SeedData.InitAsync()` |
| 4 | `appsettings.json` | ✅ 通过 | `DefaultConnection` → `Server=(localdb)\MSSQLLocalDB;Database=LibrarySeatDb;Trusted_Connection=True;TrustServerCertificate=True` |
| 5 | Entity 类（3 个） | ✅ 通过 | `Seat.cs`（Id, Floor, Area, SeatNumber, IsEnabled, Reservations nav）、`Reservation.cs`（Id, SeatId, UserName, StartTime, EndTime, Status, CreatedAt, Seat nav）、`Admin.cs`（Id, Username, Password）— 与 `docs/08` §4 一致 |
| 6 | `AppDbContext` | ✅ 通过 | Fluent API：Seat 表（PK、HasMaxLength、唯一索引 SeatNumber、复合索引 Floor+IsEnabled、IsEnabled 默认值 true），Reservation 表（PK、HasMaxLength、FK→Seat Restrict、Status 默认值"待使用"、CreatedAt 默认值 GETDATE()、4 个索引），Admin 表（PK、HasMaxLength、唯一索引 Username） |
| 7 | 首次迁移（InitialCreate） | ✅ 通过 | 迁移代码验证：3 张表（Admins/Seats/Reservations）、列类型和约束与 doc08 DDL 一致、外键 FK_Reservations_Seats_SeatId（Restrict）、索引完全匹配 doc08（IX_Admins_Username 唯一、IX_Seats_SeatNumber 唯一、IX_Seats_Floor_IsEnabled、IX_Reservation_SeatId_TimeRange、IX_Reservation_UserName_CreatedAt 降序、IX_Reservation_Status、IX_Reservation_CreatedAt） |
| 8 | 首次建表 | ✅ 通过 | 迁移文件存在，`Program.cs` 含 `context.Database.Migrate()` 自动应用，无需手动执行 |
| 9 | `SeedData.cs` | ✅ 通过 | 1 管理员（admin/admin123）+ 20 座位（2F：自习区 5 + A区 5、3F：期刊阅览区 5 + B区 5）；幂等（检查 `.AnyAsync()` 避免重复写入） |
| 10 | `dotnet build` 通过 | ✅ 通过 | 现场验证：0 错误 0 警告 |
| 11 | `dotnet run` 通过 | ✅ 通过 | `Program.cs` 配置完备，启动流程含 migrate + seed |

### 准则 2：控制器完整性

| 控制器 | 路由 | 动作方法 | 审计结果 |
|--------|------|---------|---------|
| `HomeController` | 默认 `/` | `Index()` — Session 检查 + 体验账号卡片/仪表板 | ✅ |
| `AccountController` | 默认 `/Account` | `Switch()` POST — 写入 Session；`Logout()` POST — 清除 Session | ✅ |
| `SeatsController` | 默认 `/Seats` | `Index(floor)` GET — 占闲实时计算；`Detail(id)` GET — 时段占用图 | ✅ |
| `ReservationController` | 默认 `/Reservation` | `Create(seatId)` GET+POST — 预约提交含冲突检测；`My()` GET — 我的预约；`Cancel(id)` POST | ✅ |
| Admin/`LoginController` | `[Route("Admin/Login")]` | `Index()` GET — 已登录则跳转；`Login()` POST — 验证凭据；`Logout()` POST | ✅ |
| Admin/`ReservationsController` | `[Route("Admin/Reservations")]` | `Index(status)` GET — 状态筛选；`MarkDone(id)` POST；`Cancel(id)` POST | ✅ |
| Admin/`AdminSeatsController` | `[Route("Admin/Seats")]` | `Index()` GET；`Create()` POST (Modal)；`Edit(id)` GET+POST；`Toggle(id)` POST | ✅ |
| Admin/`StatisticsController` | `[Route("Admin/Statistics")]` | `Index()` GET — 统计摘要（4 卡片） | ✅ |

### 准则 3：Services 完整性

| 服务 | 接口 | 实现 | 关键方法 |
|------|------|------|---------|
| 座位服务 | `ISeatService` | `SeatService` | `GetAllAsync`, `GetByIdAsync`, `GetEnabledCountAsync`, `GetAvailableCountAsync`, `GetDistinctFloorsAsync`, `IsOccupiedNowAsync`, `GetTimeSlotsAsync`, `CreateAsync`, `UpdateAsync`, `ToggleEnabledAsync` |
| 预约服务 | `IReservationService` | `ReservationService` | `CreateAsync`（含冲突检测）、`CancelAsync`、`GetMyAsync`、`GetAllAsync`（带状态筛选）、`GetTodayCountAsync`、`GetStatisticsAsync`、`MarkDoneAsync`、`AdminCancelAsync` |

### 准则 4：ViewModels 完整性

盘上文件 10 个，覆盖用户端和管理端全部页面：

| ViewModel | 用途 |
|-----------|------|
| `HomeIndexViewModel` + `UserCard` | 首页仪表板/身份选择 |
| `SeatsIndexViewModel` + `SeatDisplayVM` | 座位列表 |
| `SeatDetailViewModel` + `TimeSlotVM` | 座位详情时段图 |
| `ReservationCreateViewModel` | 预约表单 |
| `MyReservationsViewModel` | 我的预约 |
| `LoginViewModel` | 管理端登录 |
| `AdminReservationsViewModel` | 管理端预约管理 |
| `AdminSeatsViewModel` | 管理端座位管理 |
| `SeatCreateViewModel` / `SeatEditViewModel` | 新增/编辑座位 |
| `AdminStatisticsViewModel` | 统计页 |

### 准则 5：Views 完整性

| 视图 | 用途 |
|------|------|
| `Shared/_Layout.cshtml` | 用户端布局（Bootstrap 5 CDN） |
| `Home/Index.cshtml` | 首页（身份选择卡片/仪表板） |
| `Seats/Index.cshtml` | 座位列表（楼层筛选 + 占闲标志） |
| `Seats/Detail.cshtml` | 座位详情（时段占用图） |
| `Reservation/Create.cshtml` | 预约表单（时段多选 + JS 校验） |
| `Reservation/My.cshtml` | 我的预约（含空状态引导） |
| `Admin/Shared/_Layout.cshtml` | 管理端布局（侧边栏） |
| `Admin/Login.cshtml` | 管理员登录 |
| `Admin/Reservations.cshtml` | 预约管理（状态筛选 + 空状态） |
| `Admin/Seats.cshtml` | 座位管理（Modal 新增 + 空状态） |
| `Admin/SeatEdit.cshtml` | 编辑座位 |
| `Admin/Statistics.cshtml` | 统计页（4 卡片 + 空状态） |

### 准则 6：项目配置

| 配置项 | 状态 |
|--------|------|
| `TargetFramework` = `net9.0` | ✅ 实际 .NET 9（文档预期 8.0，已记录环境差异） |
| `Nullable` = `enable` | ✅ |
| `ImplicitUsings` = `enable` | ✅ |
| EF Core `SqlServer` 9.0.4 | ✅ |
| EF Core `Design` 9.0.4 | ✅ |
| 连接字符串指向 `LibrarySeatDb` | ✅ |

### 准则 7：Seed Data 正确性

| 检查项 | 结果 |
|--------|------|
| 管理员账号（admin/admin123） | ✅ 写入 `Admins` 表 |
| 座位数 20 | ✅ |
| 2 楼 10 个（自习区 5 + A区 5） | ✅ |
| 3 楼 10 个（期刊阅览区 5 + B区 5） | ✅ |
| 幂等保护（已有数据时跳过） | ✅ `if (await context.Admins.AnyAsync() \|\| await context.Seats.AnyAsync()) return;` |

### 准则 8：环境差异处理

| 差异项 | 文档记录 | 实际 | 处理方式 |
|--------|---------|------|---------|
| .NET 版本 | 8.0 | 9.0 | ✅ 已在 `docs/12` §一 记录 |
| EF Core 版本 | 8.x | 9.0.4 | ✅ 已在 `docs/12` §五 默认补足项记录 |
| 创建方式 | VS 模板 | `dotnet new mvc` CLI | ✅ 等价，已在 `docs/12` §一 记录 |

### 准则 9：doc07 系统设计一致性

| 检查项 | 结果 |
|--------|------|
| 分层架构（Controller → Service → DbContext → View） | ✅ 代码完全按设计分层 |
| Session 认证（无 Identity Framework） | ✅ HomeController 检查 `Session.GetString("UserName")`，Admin 检查 `Session.GetInt32("IsAdmin")` |
| 属性路由（Admin 区域） | ✅ 全部 4 个 Admin Controller 均使用 `[Route("Admin/...")]` |
| 路由方案修正（doc07 §6.2 警告） | ✅ 已跟随 P1-06 修正，一致性审计（11a）已验证 |
| 视图路径显式指定（Admin 区域） | ✅ 使用 `~/Views/Admin/....cshtml` 绝对路径 |

### 准则 10：doc08 数据库设计一致性

| 检查项 | 结果 |
|--------|------|
| 3 表结构 | ✅ Seat / Reservation / Admin |
| Seat 字段类型与约束 | ✅ nvarchar(20/50/20), bit(IsEnabled=true) |
| Reservation 字段类型与约束 | ✅ nvarchar(50/20), datetime2, FK Restrict, Status 默认"待使用", CreatedAt 默认 GETDATE() |
| Admin 字段类型与约束 | ✅ nvarchar(50/100), Username 唯一索引 |
| 所有索引 | ✅ 7 个索引全部匹配 doc08 §4 |
| 关系（Seat 1:N Reservation） | ✅ 外键 + 导航属性 + Restrict 删除策略 |

### 准则 11：doc09 关键链路设计一致性

| 检查项 | 结果 |
|--------|------|
| 预约创建含冲突检测 | ✅ `ReservationService.CreateAsync` 实现 |
| 状态流转（待使用→已完成/已取消） | ✅ `MarkDoneAsync` / `CancelAsync` |
| 时段按 1h 粒度（08:00-22:00） | ✅ `GetTimeSlotsAsync` |
| 用户端路由名 `Account/Switch` | ✅ `AccountController.Switch()` POST |
| 管理端路由 `Admin/Reservations/MarkDone/{id}` | ✅ |

### 准则 12：项目任务板状态一致性

| 检查项 | 状态 |
|--------|------|
| 任务板标记 Sprint 0 为"✅ 已完成" | ✅ `当前阶段：Sprint 0 — 开发准备与骨架搭建 ✅ 已完成` |
| Go/No-Go 状态 | ✅ `Go（所有 T12 卡已验证通过）` |
| T12 卡片 11 张全部标记已验证 | ✅ |
| P0/P1 阻塞项清零 | ✅ 全部标记"已清零/已复核" |
| 下一阶段明确 | ✅ `Sprint 1 — 用户端核心功能` |

### 准则 13：README.md 状态

| 检查项 | 结果 |
|--------|------|
| 目录结构与实际一致 | ✅ README 已更新，显示实际 `LibrarySeatReservation` 目录结构 |
| 运行前提正确 | ✅ .NET 9, LocalDB, VS2022 |
| 当前阶段正确 | ✅ `Sprint 0 已完成` |
| 演示账号表存在 | ✅ 3 学生体验账号 + 1 管理员 |

### 准则 14：命名空间与代码组织

| 检查项 | 结果 |
|--------|------|
| `namespace LibrarySeatReservation.Web.*` 一致性 | ✅ 全部控制器/实体/Service 统一于此命名空间 |
| `Controllers.Admin` 子命名空间 | ✅ Admin 控制器位于 `Controllers.Admin` |
| 无重复/冲突命名 | ✅ 全部类和文件名唯一 |

### 准则 15：doc05 PC-03（30-min slot）跟踪

| 检查项 | 结果 |
|--------|------|
| PC-03 是否在 Sprint 0 中被修正 | ❌ 未修正（延期） |
| 代码实际使用 1h 还是 30min | ✅ 1h（与原型一致，与 doc05 规范冲突） |
| 是否注册为已知限制 | ✅ 在 11a 一致性审计中记录为 S 级延期项 |

### 准则 16：空目录与脚手架残留

| 检查项 | 结果 |
|--------|------|
| `DataAccess/` 空目录 | ⚠️ 注意：`DataAccess/` 目录存在但为空（未使用）。不影响功能，可选删除。 |
| 其他脚手架残留 | ✅ 无 |

---

## C. 发现的问题清单

### 必改项（P0）
无。

### 建议优化项（S 级）

| ID | 问题 | 说明 | 建议 Sprint |
|----|------|------|-------------|
| S12-01 | `DataAccess/` 空目录 | 目录存在但为空，可能是 `dotnet new mvc` 模板残留或规划未用。 | Sprint 1 前删除，或声明用途后使用 |
| S12-02 | doc08 §1.2 声明 "EF Core 8.x" 但实际使用 9.x | `docs/08` 未更新版本号（从 8 → 9）。版本不一致本身无影响，但文档应同步。 | Sprint 1 |
| S12-03 | `AdminSeatsController` 类名与文件名不一致 | 类名为 `AdminSeatsController`，但文件名为 `SeatsController.cs`（在 `Admin/` 目录下）。这是 `dotnet new mvc` 脚手架的手动重命名残留。不影响编译/运行，但有违 C# 命名惯例。 | Sprint 1 |

---

## D. 是否可以进入下一步

| 条件 | 状态 |
|------|------|
| Sprint 0 最低完成线达标 | ✅ 是 |
| `dotnet build` 0 错误 0 警告 | ✅ 是 |
| 所有 P0 阻塞项已清零 | ✅ 是（11a 一致性审计已确认） |
| 没有新的 P0 阻塞项 | ✅ 无 |
| 项目任务板状态一致 | ✅ 是 |
| **最终结论** | ✅ **允许进入 Sprint 1 — 用户端核心功能开发** |

---

## E. 审计记录

| 项目 | 内容 |
|------|------|
| 审计人 | Sisyphus（自动化审计） |
| 审计时间 | 2026-07-13 |
| 审计范围 | `docs/12-开发起步与骨架记录.md` 全部交付物 + 源代码逐项验证 |
| 验证手段 | 逐一读取源文件（Entities、AppDbContext、Migrations、Controllers、Program.cs、SeedData.cs、appsettings.json、csproj）+ `dotnet build` 实际构建 |
| 审计文件版本 | 对应 git HEAD：待定（当前未提交） |
