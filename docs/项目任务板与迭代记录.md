# 项目任务板与迭代记录

> 本文档模拟在线任务板，按 Sprint 分区管理任务卡片。
> 编号规则：`T<两位 Sprint 编号>-<两位序号>`，如 T12-01 = Sprint 0 第 1 张卡。

---

## 当前 Sprint

**当前阶段：Sprint 0 — 开发准备与骨架搭建**（参见 `docs/10-开发准备与Sprint0.md` §4）

---

## 任务板总览（四列）

### 待开发

| 卡片 | 标题 | 优先级 |
|------|------|--------|
| T12-01 | 创建解决方案 `.sln` 与 Web 项目 `.csproj` | P0 |
| T12-02 | 安装 NuGet 包 `EF Core SqlServer` | P0 |
| T12-03 | 创建项目目录结构（Controllers / Models / Services / Views / Data） | P0 |
| T12-04 | 编写 Entity 类（Seat / Reservation / Admin） | P0 |
| T12-05 | 编写 AppDbContext + Fluent API 配置 | P0 |
| T12-06 | 配置 Program.cs（DbContext / Session / DI / Seed） | P0 |
| T12-07 | 配置 appsettings.json（连接字符串） | P0 |
| T12-08 | 首次迁移：`Add-Migration InitialCreate` | P0 |
| T12-09 | 首次建表：`Update-Database` | P0 |
| T12-10 | 编写 SeedData.cs（管理员 + 座位） | P0 |
| T12-11 | `dotnet build` + `dotnet run` 验证 | P0 |

### 进行中

> （无）

### 待验证

> （无）

### 已完成

> （无）

---

## Spring 0：开发准备与骨架搭建

### Sprint 目标

搭建项目骨架、首次运行、数据库初始化。

### 阶段最低完成线

`dotnet run` 可启动，浏览器可访问首页，3 张表已创建，Seed Data 已写入。

### 当前轮次

第 1 轮 / 共允许多轮

### 任务统计

**卡片总数**：11 张（P0：11 张）
**已完成**：0 / 11（0%）
**完成率**：0%

### 是否允许进入下一阶段

❌ **未达到最低完成线**（需至少完成 T12-01~T12-11 中全部 P0 卡）

---

### 任务卡详情

#### T12-01：创建解决方案 `.sln` 与 Web 项目 `.csproj`

| 项目 | 内容 |
|------|------|
| **验收标准** | `LibrarySeatSystem.sln` 存在；`LibrarySeatSystem/LibrarySeatSystem.csproj` 存在；VS 或 `dotnet build` 可打开 |
| **关联文档** | `docs/10-开发准备与Sprint0.md` §4.3 |
| **技术操作** | `dotnet new sln` → `dotnet new mvc` → `dotnet sln add` |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-02：安装 NuGet 包 `EF Core SqlServer`

| 项目 | 内容 |
|------|------|
| **验收标准** | `LibrarySeatSystem.csproj` 中包含 `Microsoft.EntityFrameworkCore.SqlServer` 引用 |
| **关联文档** | `docs/08-数据库设计.md` §1.2 |
| **技术操作** | `dotnet add package Microsoft.EntityFrameworkCore.SqlServer` |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-03：创建项目目录结构

| 项目 | 内容 |
|------|------|
| **验收标准** | 目录结构包含 `Controllers/Admin/`、`Models/Entities/`、`Models/ViewModels/`、`Services/`、`Views/Admin/Shared/`、`Data/`、`wwwroot/css/` |
| **关联文档** | `docs/07-系统设计说明.md` §3.1 |
| **技术操作** | `mkdir` 命令创建目录 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-04：编写 Entity 类（Seat / Reservation / Admin）

| 项目 | 内容 |
|------|------|
| **验收标准** | 3 个 Entity 类的字段、类型、默认值与 `docs/08-数据库设计.md` §4 完全一致；导航属性正确配置 |
| **关联文档** | `docs/08-数据库设计.md` §4.1~4.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-05：编写 AppDbContext + Fluent API 配置

| 项目 | 内容 |
|------|------|
| **验收标准** | `DbSet<Seat>`、`DbSet<Reservation>`、`DbSet<Admin>` 声明；唯一索引、复合索引、默认值、外键 `Restrict` 均已配置；与 `docs/08-数据库设计.md` §4 Fluent API 一致 |
| **关联文档** | `docs/08-数据库设计.md` §4 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-06：配置 Program.cs（DbContext / Session / DI / Seed）

| 项目 | 内容 |
|------|------|
| **验收标准** | `AddDbContext`、`AddSession`（含 30 分钟超时）、`AddScoped<ISeatService, SeatService>`、`AddScoped<IReservationService, ReservationService>`、`app.UseSession()`、Seed 调用均已注册；编译通过 |
| **关联文档** | `docs/07-系统设计说明.md` §6.2、§9.4 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-07：配置 appsettings.json（连接字符串）

| 项目 | 内容 |
|------|------|
| **验收标准** | `ConnectionStrings.DefaultConnection` 指向 `(localdb)\MSSQLLocalDB;Database=LibrarySeatDb` |
| **关联文档** | `docs/07-系统设计说明.md` §2 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-08：首次迁移（Add-Migration InitialCreate）

| 项目 | 内容 |
|------|------|
| **验收标准** | `Migrations/` 目录生成；迁移文件中 3 张表的列定义与 `docs/08-数据库设计.md` 附录 A DDL 一致（表名、列名、类型、索引、外键） |
| **关联文档** | `docs/08-数据库设计.md` 附录 A、附录 B |
| **技术操作** | `dotnet ef migrations add InitialCreate` |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-09：首次建表（Update-Database）

| 项目 | 内容 |
|------|------|
| **验收标准** | 在 SSMS 或 VS SQL Server 对象资源管理器中可以看到 `LibrarySeatDb` 数据库，包含 3 张表（Seats、Reservations、Admins） |
| **技术操作** | `dotnet ef database update` |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-10：编写 SeedData.cs

| 项目 | 内容 |
|------|------|
| **验收标准** | 应用启动后自动写入 1 个管理员（admin / admin123）和 15~20 个座位；数据库中可查询到这些数据；与 `docs/08-数据库设计.md` §10.3 规格一致 |
| **关联文档** | `docs/08-数据库设计.md` §10.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T12-11：`dotnet build` + `dotnet run` 验证

| 项目 | 内容 |
|------|------|
| **验收标准** | `dotnet build` 无错误、无警告；`dotnet run` 后可访问 `http://localhost:5000`，页面正常渲染（默认 Home/Index）；数据库表中有 Seed Data |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

---

## Sprint 1：用户端核心功能

### Sprint 目标

完整走通用户预约主链路。

### 阶段最低完成线

浏览器可完成一次"选择身份 → 浏览座位 → 查看详情 → 提交预约 → 我的预约 → 取消"的完整流程。

### 当前轮次

第 0 轮 / 未开始

### 任务统计

**卡片总数**：7 张（P0：7 张）
**已完成**：0 / 7（0%）
**完成率**：0%

### 是否允许进入下一阶段

⏳ **未开始**

---

### 任务卡详情

#### T13-01：首页身份选择界面（HomeController.Index + View）

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/` → 未选身份时展示 3 张用户卡片（学生 A/B/C）；点击卡片 → POST `/Account/Switch` → 写入 Session → 跳转到座位列表 |
| **关联页面** | 用户首页 `Views/Home/Index.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.1、§2.3.2 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-02：首页仪表板模式（登录后概览数据）

| 项目 | 内容 |
|------|------|
| **验收标准** | 已选身份后访问 `/` → 显示总座位数、空闲座位数、今日预约数（从数据库实时查询） |
| **关联页面** | 用户首页 `Views/Home/Index.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.1、§2.4.1（GetEnabledCountAsync / GetAvailableCountAsync / GetTodayCountAsync） |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-03：座位列表 + 占闲状态 + 楼层筛选

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/Seats` → 加载 15~20 个座位，每个显示编号、楼层、区域、空闲/占用；切换楼层下拉 → 列表只显示对应楼层座位；未选身份时可浏览但预约按钮提示选身份 |
| **关联页面** | 座位列表 `Views/Seats/Index.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-04：座位详情 + 时段占用图

| 项目 | 内容 |
|------|------|
| **验收标准** | 点击座位 → `/Seats/Detail/{id}` 展示完整信息；时段占用图显示当天 08:00-22:00 每个 slot 是否被占；座位已禁用时隐藏"预约此座位"按钮 |
| **关联页面** | 座位详情 `Views/Seats/Detail.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.4、§2.4.1（GetTimeSlotsAsync） |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-05：预约提交 + 冲突检测

| 项目 | 内容 |
|------|------|
| **验收标准** | 进入 `/Reservation/Create?seatId=3` → 选择日期 + 勾选时段 → 提交 → 检测通过则写入数据库并跳转到我的预约；冲突时返回当前页显示错误提示；未选时段时客户端 + 服务端双重校验 |
| **关联页面** | 预约提交 `Views/Reservation/Create.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.5、§2.4.2（CreateAsync） |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-06：我的预约列表

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/Reservation/My` → 显示当前用户所有预约记录（座位编号、日期、时段、状态），按创建时间倒序；"待使用"的记录显示"取消预约"按钮；无记录时展示空状态 |
| **关联页面** | 我的预约 `Views/Reservation/My.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.6 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T13-07：取消预约

| 项目 | 内容 |
|------|------|
| **验收标准** | 我的预约中点击"取消预约"→ 确认弹窗 → 确认后状态变为"已取消"；取消后同一座位同一时段可重新预约；已完成/已取消的预约无取消按钮 |
| **关联页面** | 我的预约 `Views/Reservation/My.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §2.3.7、§2.4.2（CancelAsync） |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

---

## Sprint 2：管理端核心功能

### Sprint 目标

管理员可登录、查看/筛选预约、标记完成/取消、管理座位。

### 阶段最低完成线

管理员登录后可完成一次"标记完成"和一次"新增座位"。

### 当前轮次

第 0 轮 / 未开始

### 任务统计

**卡片总数**：7 张（P0：7 张）
**已完成**：0 / 7（0%）
**完成率**：0%

### 是否允许进入下一阶段

⏳ **未开始**

---

### 任务卡详情

#### T14-01：管理员登录 + 退出

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/Admin/Login` → 表单；输入 admin / admin123 → 登录成功跳转到预约管理；输入错误密码 → 提示"用户名或密码错误"；未登录直接访问管理端页面 → 自动跳转到登录页 |
| **关联页面** | 管理员登录 `Views/Admin/Login.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.1 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-02：预约管理列表 + 状态筛选

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/Admin/Reservations` → 显示所有用户的预约列表；下拉筛选可过滤"全部/待使用/已完成/已取消"；无数据时展示空状态 |
| **关联页面** | 预约管理 `Views/Admin/Reservations.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.2 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-03：标记完成 + 管理员取消

| 项目 | 内容 |
|------|------|
| **验收标准** | 对"待使用"预约 → 点击"标记完成" → 状态变为"已完成"；点击"取消" → 状态变为"已取消"；对已完成/已取消的记录 → 无操作按钮；操作失败时 `TempData` 提示 |
| **关联页面** | 预约管理 `Views/Admin/Reservations.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.2、§3.4 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-04：座位管理列表

| 项目 | 内容 |
|------|------|
| **验收标准** | 访问 `/Admin/Seats` → 显示所有座位的完整信息（含已禁用）；每条记录右侧有"编辑"和"启用/禁用"按钮 |
| **关联页面** | 座位管理 `Views/Admin/Seats.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-05：新增座位

| 项目 | 内容 |
|------|------|
| **验收标准** | 点击"新增座位"→ 填写楼层/区域/座位号 → 提交后出现在列表中；编号重复时提示"座位编号已存在" |
| **关联页面** | 座位管理 `Views/Admin/Seats.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-06：编辑座位

| 项目 | 内容 |
|------|------|
| **验收标准** | 点击"编辑"→ 加载现有数据到表单 → 修改后提交 → 列表数据更新 |
| **关联页面** | 座位管理编辑页 `Views/Admin/SeatEdit.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

#### T14-07：启用/禁用座位

| 项目 | 内容 |
|------|------|
| **验收标准** | 点击"禁用"→ `IsEnabled = false` → 该座位在用户端不再显示；点击"启用"→ 恢复显示；不存在的座位 ID → 提示错误 |
| **关联页面** | 座位管理 `Views/Admin/Seats.cshtml` |
| **关联文档** | `docs/09-关键链路详细设计.md` §3.3.3 |
| **当前状态** | ⏳ 待开发 |
| **本轮验证通过** | — |

---

## Sprint 3：联调与缺陷修复

### Sprint 目标

全链路稳定、审计修复落实、13 个测试用例全部通过。

### 阶段最低完成线

`docs/09-关键链路详细设计.md` §4.5 的 **13 个测试用例** 全部通过，手动验证可走通。

### 当前轮次

第 0 轮 / 未开始

### 任务统计

**卡片总数**：待 Sprint 1+2 完成后根据实际缺陷情况确定

### 是否允许进入下一阶段

⏳ **未开始**

---

### 任务卡详情（前置依赖完成后再细化）

| 卡片 | 标题 | 说明 |
|------|------|------|
| T15-01 | 执行 TC01~TC06 用户链路测试 | 6 个用例逐项验证、截图记录 |
| T15-02 | 执行 TC07~TC13 管理端链路测试 | 7 个用例逐项验证、截图记录 |
| T15-03 | 落实 6 项静态原型审计修复 | 检查 `docs/07-系统设计说明.md` §10.4 每项修复是否已在代码中落实 |
| T15-04 | N+1 查询优化（批量方法） | 实现 `GetOccupiedSeatIdsAsync`，替代逐条查询 |
| T15-05 | 管理端手机端适配验证 | 窄屏下侧边栏隐藏、表格横向滚动 |
| T15-06 | Session 超时与权限边界验证 | 超时后访问各页面，行为是否符合预期 |
| T15-07 | 编译警告修复 | `dotnet build` 零警告 |

---

## Sprint 4：缓冲 Sprint（P2 功能 + 样式 + 文档）

### Sprint 目标

统计页（如有余力）、样式调整、文档最终更新。

### 阶段最低完成线

Sprint 3 产出若已稳定，Sprint 4 可为空跑。核心验收标准仍是 Sprint 3 的 13 个测试用例。

### 当前轮次

第 0 轮 / 未开始

### 任务统计

**卡片总数**：3 张（P2：3 张）

### 是否允许进入下一阶段

⏳ **未开始**

---

### 任务卡详情

| 卡片 | 标题 | 说明 |
|------|------|------|
| T16-01 | 统计页（P2） | 实现 `/Admin/Statistics`，展示今日预约数/有效预约数/使用率 |
| T16-02 | Bootstrap 样式微调 | 调整配色、间距、对齐，确保页面整洁 |
| T16-03 | 最终 README + 部署文档 | 更新 README 为最终版本，补充部署说明 |

---

## 个人站会记录模板

### 站会格式要求

- 每天一次，5~10 分钟
- 记录在下方表格中

| 日期 | 昨天完成 | 今天计划 | 阻塞项 |
|------|---------|---------|--------|
| YYYY-MM-DD | — | — | — |
| YYYY-MM-DD | — | — | — |

---

## Sprint 评审记录模板

### 评审格式要求

- 每个 Sprint 结束时一次
- 演示可工作功能，不读文档

| 项目 | 内容 |
|------|------|
| **Sprint** | Sprint X |
| **评审日期** | YYYY-MM-DD |
| **演示链路** | 列出演示的功能链路 |
| **通过情况** | ✅ 全部通过 / ⚠️ 部分通过 / ❌ 未通过 |
| **未通过项** | 逐条列出未达标的验收标准 |
| **备注** | 阻塞项、依赖项说明 |

---

## Sprint 回顾记录模板

### 回顾格式要求

- 每个 Sprint 结束时一次
- 三列：Keep / Problem / Try

| 项目 | 内容 |
|------|------|
| **Sprint** | Sprint X |
| **回顾日期** | YYYY-MM-DD |
| **Keep（继续做）** | — |
| **Problem（问题）** | — |
| **Try（尝试改进）** | — |

---

## 附录：任务卡状态标记说明

| 标记 | 含义 |
|------|------|
| ⏳ 待开发 | 任务未开始 |
| 🔄 进行中 | 任务正在执行 |
| ✅ 待验证 | 开发完成，等待验证 |
| ✅ 已验证 | 验收标准通过 |
| ❌ 阻塞 | 因外部依赖无法继续 |

---

> **本文件为活文档**，每轮开发时更新状态、追加记录。
> 下一阶段：开发前一致性总审计 → Sprint 0 执行。
